# Please uncomment and fill out all the variables
# Each variable should have a clear explanation.

# Your email address.  Used to track who launched the instance.

# email="jon@jonhaddad.com"

# Default region to launch instances in
# We will automatically grab the right Ubuntu AMI for the region (check the map in configuration.tf)

# region = "us-west-2"


# We should probably just create a default C* group for cluster testing

#security_groups = ["haddad"]

# AWS key.  We will automatically update the authorized_keys file in aws_cluster/config
#key_name = "haddad2"

/*
You should have an AWS profile configured via the aws command line tool
It gets brought into the docker container automatically
Put the profile name below
*/

#profile = "tlp"
